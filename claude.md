---
class: live
---

# claude.md — Trinity Local

> **Own your taste — Trinity picks the answer you would have picked.**

Hero: *"Own your taste. Lives inside Claude Code, Codex CLI, Antigravity, and Cursor."*

Sub: *"No new app. No service. No API key. Your transcripts never leave your machine."*

Trinity is an MCP server that reads your existing CLI transcripts (Claude Code, Codex, Antigravity, Cursor — plus claude.ai / chatgpt.com / Gemini Takeout exports), extracts a **lens** (the pattern in how you rephrase, judge, and decide), and runs hard questions through all three frontier providers in your voice. The chairman synthesizes a single answer the user would have picked. Status: v1.7.4 soft-launched May 13–15, 2026; v1.7.5 (rating retirement + Auto-Dream cite) shipped 2026-05-22, v1.7.6 (Trinity-heals-itself `extension repair` demo) shipped 2026-05-23, v1.7.7 (gemini.google.com capture: XHR interception lands + content-script context-invalidation guard) shipped 2026-05-23, v1.7.8 (gemini per-RPC `file_stem` so multiple RPCs per turn land distinctly instead of overwriting — unblocks #144 verification) shipped 2026-05-23, v1.7.9 (gemini parser fix: brace-depth scan beats Google's unreliable length prefix — closes #144) shipped 2026-05-23, and v1.7.10 (provider-side memory loop end-to-end: lens-prompt/lens-import + eval-prompt/eval-import CLI verbs with `--provider` override, launchpad CTAs, eval-show/eval-share `--compare` leaderboard, `import_provider_memory` MCP tool + SKILL.md activation hint — 7 commits ratified by council `3e4564e9` and dogfooded on real data) shipped 2026-05-25, v1.7.11 (e2e Chrome dogfood arc — fixed 9 production bugs via real-Chrome dispatch testing) shipped 2026-05-26, v1.7.12 (seed-kernel recursion explicit — T3 reads lens tensions AS its rubric, T3↔T4 calibration, gate-over-lens primitive — see [CHANGELOG.md v1.7.12](CHANGELOG.md)) shipped 2026-05-27 morning, and v1.7.13 (moves teardown; -4400 LOC), v1.7.14 (T2 lens validation activates), v1.7.15 (post-moves dead-code cleanup; -841 LOC), v1.7.16 (orphan-finder CI guard), v1.7.17 (retirement denylist expansion — AST guards block imports from retired modules + retired CLI flags; doc-consistency guards 105→107), v1.7.18 (round-trip persistence guard; gstack triage found 4 of 5 patterns don't fit), v1.7.19 (Stage 4 T2 filter skips under TF-IDF fallback — #185 caught a latent #186 bug), v1.7.20 (doc-side retired-name guard; doc-consistency guards 107→108), v1.7.21 (output-shape smoke for live producers — closes #191's 2nd promotion), v1.7.22 (lens-build clobber guard — save_rejections refuses to overwrite a populated corpus with a degenerate Stage 0 result), and v1.7.23 (Stage 0 prompt chunking — the actual root cause: 200 turn-pairs packed into one 37K-token prompt that `claude -p` returned empty for; now batched 40/call, parse-without-save, single final save; unblocks #186 T2 validation on real data; see [CHANGELOG.md](CHANGELOG.md)) shipped 2026-05-28, and v1.7.24 (lens accumulation core — tension registry keyed by embedding-cosine identity + Stage 4.5 reconcile; the lens now accumulates across rebuilds instead of replacing its surface tensions, with first-phrasing-wins stability + recency decay; build-step-1 of `docs/lens-redesign.md`; see [CHANGELOG.md](CHANGELOG.md)) shipped 2026-05-28, and v1.7.25 (accretion goes visible — lens.md renders each tension's support count + stability provenance from the registry, with the n<3 low-confidence caveat; the RENDER half of #197) shipped 2026-05-28, and v1.7.26 (lens-resync — `lens-resync` CLI verb seeds the tension registry from existing lenses.json + re-renders lens.md with support, no chairman calls; build-step-2 migration, browser-verified in the memory viewer) shipped 2026-05-28, and v1.7.27 (accumulation chip on the launchpad lens card — `_load_taste_lenses` enriches each tension with registry support; the card shows "N decisions" + low-confidence flag + stability tooltip; RENDER verb complete across surfaces, playwright-verified) shipped 2026-05-28, and v1.7.28 (find_anchors fix — vocabulary anchors were 100% Trinity's own captured prompt scaffolding; prevalence cap (IDF, gated on ≥20 threads) + transcript_id attribution fix + imperative blacklist; real anchors now surface (LDK/Kitchen/Bath/…); unblocks lens-redesign Stage 4.25 vocab canonicalization; browser-verified) shipped 2026-05-28, and v1.7.29 (self-review fixes on the accumulation arc — recursive blacklist strip in find_anchors, duplicate-tension_id guard in reconcile, launchpad chip reads full registry not active-only) shipped 2026-05-28, and v1.7.30 (EXTRACT unification Stage 1 — `me/preference_acts.py` unifies rejections (model_miss) + decisions (self_expressed) into one `PreferenceAct` type with adapters + `iter_preference_acts` reader; lens.md renders both as one "Preference acts" section so decisions reach the chairman for the first time; Strangler-Fig — writers/eval/storage unchanged, extraction-merge + storage-migration are later stages per `docs/lens-redesign.md`; browser-verified) shipped 2026-05-28, and v1.7.31 (EXTRACT unification Stage 2 — eval-build sources through `iter_preference_acts()` (model_miss subset) instead of raw rejections.jsonl dicts; behavior-preserving (same 49 eval items), `load_rejections` defaults missing user_substitute to ""; chose this safe reader-migration over the quality-risky prompt-merge) shipped 2026-05-28, and v1.7.32 (EXTRACT unification Stage 3 — the unified `preference_acts.jsonl` ledger as a canonical export written by lens-build/lens-resync; `save/load_preference_acts` + `lens-acts` introspection verb; `iter_preference_acts` still reads the legacy union so it can't go stale — the read-path flip + legacy retirement are the final stage; dogfooded 98 acts) shipped 2026-05-28, and v1.7.33 (clobber guard on the unified `preference_acts.jsonl` ledger — `save_preference_acts` refuses a degenerate overwrite of a populated ledger (#194 pattern) before the read-path flip makes it load-bearing) shipped 2026-05-28, and v1.7.34 (adopt Claude Opus 4.8 — `claude-opus-4-8` is the default Claude model in config.example.json (top-level + bundled) + the provider-trio table + sync_reference_evals; reference_evals.json keeps its recorded 4.7 numbers until re-synced) shipped 2026-05-28, and v1.7.35 (default provider effort max/xhigh → high in both config.example copies — peak effort was why lens-build crawled; high is ~as good for council synthesis at much lower latency; per-stage extraction override is a separate deferred task; multi-agent review findings filed as #203–#207) shipped 2026-05-28, and v1.7.36 (fix all 5 multi-agent review findings #203–#207 — #203 HIGH: Stage 0 partial-batch silent corpus degradation now aborts via `_stage0_batch_failed` instead of saving a partial set past the #194 guard; + save_registry OSError propagation, support_index pole-collision, load_preference_acts crash-proofing, find_anchors soft-lead "new"/"one", test hardening; all tested) shipped 2026-05-28, and v1.7.37 (per-stage low effort — Stage 0/2 mechanical extraction runs at `effort=low` via an `extractor` provider regardless of council effort, + `_STAGE0_BATCH_SIZE` 40→20; a real run proved `claude -p` dispatch works from a nested CLI (distill completed) but Stage 0 at high effort hit the 8-min timeout, which #203 caught cleanly) shipped 2026-05-28, and v1.7.38 (lens-build optimization — #1 skip-if-unchanged: a `count:sha1(prompt_ids)` corpus fingerprint in `lens_build_state.json` makes a no-corpus-change rebuild return in seconds with 0 model calls (validated live at 4.4s), `--force` bypasses it; + #3 parallel Stage 0: turn-pair batches dispatch through a `ThreadPoolExecutor` capped at 4, with #203's abort-on-any-failed-batch semantics preserved; full delta-extraction is the follow-on) shipped 2026-05-28, and v1.7.39 (EXTRACT Stage 4a — read-path flip onto the unified `preference_acts.jsonl` ledger + the HIGH bug it surfaced: `parse_rejections` trusted the chairman's batch-local `r_001` sequence ids, so chunked Stage 0 (#195) collided 8 distinct rejections onto one id (59 rows → 16 ids on real data); fixed with content-hash ids via `stable_id`. `iter_preference_acts` reads the ledger content-keyed-merged with the legacy stores as a PURE read (no write-on-read); `_load_decisions_by_id` + eval-import sourced/dual-written through the ledger; validated on real data — 93 distinct acts recovered where id-keying would have lost 43; Stage 4b retires the legacy split) shipped 2026-05-28, and v1.7.40 (fix 4 HIGH bugs from the 9-agent review — recorded-model≠dispatched-model (`_reconcile_model_arg` lifts inline `--model` into the authoritative `config.model` at load, fixing routing-table + benchmark-claim poisoning), bare-JSON routing fallback truncation (brace-depth scan back-ported from v1.7.9, replacing the non-greedy regex that stopped at the first `}`), ingest-cursor equal-mtime data loss (inclusive `>=` boundary + id-dedup), and empty-embedding shadow (`_iter_jsonl_latest_by_id` `protect_field="embedding"` so a cheap empty node can't shadow a real vector)) shipped 2026-05-28, and v1.7.41 (council-first re-lead + doc consolidation — README now leads with the council painkiller ("Stop tab-hopping between Claude, ChatGPT, and Gemini" + a concrete sample verdict surfacing disagreed_claims), "Own your taste" demoted to the compounding-moat beat, + the Claude+Codex honest-minimum note (Q3) and free-for-individuals/Teams-is-revenue line (Q6); fixed all 23 genuinely-broken relative md links (specs/scale-plan → `docs/historical/`), bumped an illustrative 4.7 quote to 4.8, added `class: live` frontmatter to the provider-prompt docs, deleted the retired-moves `move.example.md` orphan; founder GTM decisions captured in memory `product_relead_council_first`; doc merges + historical class-retags remain as #215 follow-on) shipped 2026-05-28, and v1.7.42 (model-launch detection + provider-name aliases — slice 1 of the Q7 detect→notify→eval→eval-card loop: `resolve_provider_alias` accepts gemini/gpt/chatgpt/etc. and resolves to the internal slug, wired into `eval-run --target`/`--judge` (fixes the `eval-run --target gemini` breakage, Q5); a version-controlled `data/models.json` manifest rides Trinity releases as the local-first new-model signal, and `models.detect_new_models()` diffs it against each provider's last-evaluated `target_model` (authoritative post-v1.7.40) to surface a "score it against your taste" nudge in `status` — no new verb, per the Q4 surface-collapse; launchpad banner + auto eval-card are slice 2) shipped 2026-05-28, and v1.7.43 (model-launch loop slice 2 — `build_page_data` carries `newModels` and the launchpad renders a celebratory new-model card (per-model name + what's-new + click-to-copy `eval-run --target <slug>` chip) that self-hides once every current model is scored; eval-run completion leads with "🎉 You scored the latest model!" + front-doors `eval-share` when the run scored the provider's current manifest model — the viral lab-impossible eval-card; notify→share now live on both CLI `status` and the launchpad) shipped 2026-05-28, and v1.7.44 (review follow-ups — #216: `ingest_recent` records the fully-drained boundary file's `(path, size)` in `cursors.json` and skips re-parsing it while unchanged, killing the `>=`-boundary re-parse cost on the 1s `ask` MCP path (grown file re-parsed, same-mtime sibling still scanned — equal-mtime safety intact); #217: `review.py` `_reviewer_command_for` re-injects the reconciled `config.model` (post-v1.7.40 the loader strips inline `--model`), skipping antigravity; + a store.py `protect_field` whole-record-suppression clarifier) shipped 2026-05-28, and v1.7.45 (doc consolidation #215 — retired-rating copy sweep on the launchpad routing/cortex cards ("Ratings"→"Routing", "rated"→"run") + a `test_launchpad_rating_copy_retired` guard so the sunset vocabulary can't resurface; doc-class retags to `historical` for 5 completed audits (CUT-CANDIDATES/PARASITISM-AUDIT/architectural-gaps/launcher-patterns/design-frame) + 4 relocated specs; the physical audit-doc relocations into `docs/historical/` + the two content-merges (frontend-architecture→DESIGN, launch→launch-package) were judgment-deferred as low-user-value / high-coupling) shipped 2026-05-28, and v1.7.46 (cold-start aha #212/Q2 — `cold_open_tension()` surfaces ONE surprising true decision-*axis* from the lens (highest-support tension the user keeps navigating, not a fixed winner) the instant the lens has signal; the auto-scan already kicks on first MCP connect, this adds the insight. Surfaced, no new verb (Q4): launchpad hero 🪞 cold-open line (real-Chrome verified — rendered "executable artifact vs explanatory description — seen across 17 of your decisions", no console errors), a `status` "Lens insight" line, and a `lens_cold_open` field on MCP dict payloads) shipped 2026-05-28, and v1.7.47 (EXTRACT unification Stage 4b #202/#209 — the legacy split retires: `rejections.jsonl` + `decisions.jsonl` are no longer written or read, `iter_preference_acts()` is a pure read of the unified `preference_acts.jsonl` ledger as the SOLE store, and the 6 functions that touched the legacy files (`save/load/path` × rejections/decisions) are deleted + registered in `retired_names.py`; the #194/#203 degenerate-Stage-0 clobber guard moved onto `save_preference_acts` + a `me_builder` ledger-count abort; Stage 0/2 parse functions are now pure (no `save=`); `in_thread_overwrite` merge-telemetry retired with `save_rejections`; behavior-preserving on the 98-act real ledger) shipped 2026-05-28, and v1.7.48 (Stage 0 full delta-extraction #210 — beyond v1.7.38's skip-if-unchanged whole-build gate: when the corpus GREW, Stage 0 classifies only the NEW turn-pairs instead of re-sending the whole 200-pair window to the chairman. `lens_build_state.json` gains `extracted_pair_ids` (the pairs already classified, backward-compatible with the fingerprint-only file); prior rejections are reloaded from the ledger via `to_rejection` (the inverse adapter) and merged with new ones by content-stable id, so the ledger stays whole while only the delta hits the chairman; #203 abort + #194 degenerate-clobber guards preserved against the ledger's model-miss count; `--force` re-extracts everything; proven by an integration test — 2nd build after +1 pair classifies only the new pair) shipped 2026-05-29, and v1.7.49 (Q4 surface-collapse #213 slice 1 — `lens` + `council` are now the primary user-facing CLI verbs (aliases `lens-build` / `council-launch` keep launchpad dispatch + extension allowlist + copy-paste strings resolving); `trinity-local --help` leads product-first: lens, council, dream, status, install; routing dispatches on `args.handler` so the aliases carry zero behavioral risk; launchpad-card demotion + the MCP `route`→`ask` merge are deferred follow-on slices) shipped 2026-05-29, and v1.7.50 (fix 4 findings from the 9-agent adversarial review of tonight's ships #219 — MEDIUM: `lens --force` silently dropped provider-imported model_miss acts (`prompt_id=None`, not turn-pair-derivable) because force clears the carry-forward; the final ledger save now re-attaches any no-prompt_id ledger model_miss act the build didn't reproduce. MEDIUM: #209 shipped no legacy→ledger migration, so an upgrade read empty until lens-build and `lens-resync` round-tripped the empty ledger; added idempotent `_migrate_legacy_preference_stores()` invoked at lens-build (pre-fingerprint-skip), lens-resync, eval-build, recovering self-expressed decisions too. LOW: batch-failed abort telemetry undercount + stale "legacy stores" docstrings) shipped 2026-05-29, and v1.7.51 (Q4 surface-collapse #213 slice 2 — the launchpad / memory-viewer / status / me-card now hand users `trinity-local lens` and `trinity-local council --task` in every command-to-run string; three string classes kept distinct: commands-to-run shortened, provenance labels (`verdict==='accepted' ? 'lens-build'`, "Written by lens-build") kept, internal dispatch (capture_host ACTION_ALLOWLIST `council-launch`, `command_for_dispatch`) untouched; browser-verified Surfaces 18/19/20 copy the short forms, regenerated launchpad_example.png + me_card_example.png; only the MCP `route`→`ask` merge remains) shipped 2026-05-29, and v1.7.52 (Q4 surface-collapse #213 slice 3 — MCP `route` merged into `ask` as `mode="route"` (returns the routing decision, no model call); the standalone `route` tool stays registered but marked DEPRECATED (external harnesses call by name — the 8-tool contract is unchanged); `ask` schema gains `mode`/`budget`/`latency`/`current_provider`/`harness`; **#213 now complete** across CLI + launchpad + MCP) shipped 2026-05-29, and v1.7.53 (schema versioning + forward migration runner #183 — a single monotonic `schema_version` in `~/.trinity/.trinity-version` (missing = v0), a `Migration` registry + `run_migrations()` that walks forward at `main()` startup (covers CLI + the `--mcp` server it spawns), fail-safe (a raised migration leaves the marker at the last success + the runner never raises), first migration v0→v1 makes the #209 legacy→ledger recovery a run-once schema step, `status` reports `schema_version {recorded,current}`; single-doc not per-shape, inline-at-startup not a separate verb; unblocks #182) shipped 2026-05-29, and v1.7.54 (Chrome extension per-harness paste-in snippet generator #166 — `browser-extension/harness-snippets.js`, a chrome-free single-source-of-truth module for the 6 harness MCP config blocks (Claude Code/Desktop, Codex CLI, Cursor, Antigravity, Cline), all uvx-based; the popup setup card renders harness pills → click → file path + copyable config (`mcpServers` JSON for most, `[mcp_servers.trinity-local]` TOML for Codex); real-Chrome verified pill-click → correct block/path; Python guards pin harnesses/uvx/JSON-vs-TOML/paths/chrome-free/popup-wiring; "detected pills" deferred to Phase B needing NM-host filesystem access) shipped 2026-05-29, and v1.7.55 (trajectory lens — diachronic arc-pair extraction #182 — the asymmetric advantage no within-session memory (Auto-Dream incl.) can see: within ONE thread, did the user steer the SAME direction repeatedly across turns? `me/arc_mining.py` `TurnArc` (one rejection kind recurring ≥`MIN_ARC_LEN`=3 in a transcript) + `Trajectory` (kind aggregated across threads); detection is DETERMINISTIC — no LLM (group model_miss acts by originating transcript via prompt_id→PromptNode, find ≥3-of-a-kind runs); wired into lens-build + lens-resync, new lens.md "## Trajectories" section, persisted to `arcs.jsonl`/`trajectories.jsonl`; chairman-enrichment path (`render_arc_prompt`/`parse_trajectories`) built+tested as the follow-on; real-data validation — all 63 model_miss acts resolve to transcripts but max same-kind-per-thread is 2 (threshold 3), so 0 arcs yet (TRUE empirical result, threshold NOT lowered to manufacture); registered in the #190 round-trip guard) shipped 2026-05-29, and v1.7.56 (fix 2 silent new-user bugs on the extension-first install path, found by running both onboarding paths from a clean fake-home — the terminal/Claude-Code path worked end-to-end but the extension capture path was dead on a fresh install: (1) the capture-host wrapper `exec`ed bare `python3.12`, which Chrome's sanitized native-messaging PATH can't resolve when python is Homebrew-installed → install.sh now bakes the ABSOLUTE interpreter via `command -v`; (2) the wrapper ran `capture_host.py` as a script but it uses relative imports → switched to `python -m trinity_local.capture_host`. Verified end-to-end: under a Chrome-like sanitized PATH the host launches + completes a real native-messaging round-trip. Regression guards in `test_install_wrapper_uses_resolver.py`) shipped 2026-05-29, and v1.7.57 (extension auto-wire — makes "install the extension and capture just works" real, no second command: a Chrome extension can't write its own native-messaging host manifest, so install.sh pre-registers the host for ONE fixed `registry.CANONICAL_EXTENSION_ID` ahead of time (inert until an extension with that exact id connects → safe); `install-extension` with no `--extension-id` now defaults to the canonical id + writes the manifest (was: print Load-unpacked + no-op), install.sh calls it best-effort post-MCP, `registry.CHROME_WEB_STORE_URL` (empty until publish) flips the launchpad card from sideload to one-click "Add to Chrome" via `_browser_extension().webStoreUrl`, terminal `council` offers to open the result (TTY-gated); `test_extension_id_sync` keeps the bash resolver default in lockstep; verified end-to-end in a fake home) shipped 2026-05-29, and v1.7.58 (activity-gated lens refresh — Anthropic's Auto-Dream trigger (24h + 5 sessions), NOT a nightly cron: `cold_start.maybe_kick_lens_refresh()` runs at MCP connect (an authenticated session, so provider CLIs are live — no 3am-no-auth cron, no OS scheduler, no surprise spend); `should_refresh_lens()` opens the gate iff a lens exists AND ≥`REFRESH_MIN_AGE_H`=24h since the last build AND ≥`REFRESH_MIN_NEW_PROMPTS`=5 new prompts (both already in `lens_build_state.json`: `built_at` + the `count:sha1` fingerprint), then background-kicks the delta lens rebuild with a 30-min cooldown; cold-start owns the FIRST build, this owns keep-current; result recorded to `lens_refresh.json`; all paths best-effort) shipped 2026-05-29, and v1.7.59 (sharper hero copy — README + `docs/index.html` Trinity card retuned to the founder's tighter hero: "Own your taste." + "Stop copy-pasting prompts between tabs like an animal. Ask once. Trinity fans it out to Claude, GPT, and Gemini. Shows you where they split. Makes the call the way your taste would." + "Just an MCP and a Chrome extension — no new app, no cloud, no API key."; keeps the guard-pinned `Own your taste.` hero + the canonical privacy sub; no code change) shipped 2026-05-29, and v1.7.60 (agy `--model` dispatch fix + the project-lens map — the council-dogfood ship: fixed a regression where Trinity injected `--model` into `agy -p` (agy has no such flag → exit 2, every antigravity council/eval member silently failed in 0.07s); `--model` injection is now allowlisted to claude in `CLIProvider` (codex injects via `CodexProvider`'s own path), mirroring the `--effort` gate, + 3 regression guards incl. `test_no_model_flag_injected_for_antigravity` (the prior antigravity test only checked `--effort`, which is how it slipped); rendered `docs/project-lens.md` — the descriptive project-lens the dogfood workflows extracted from Trinity's own fix-history + 9 guard families (6 invariants → failure-mode tensions with support counts: doc-drift 117, recorded≠dispatched 21 — the `--model` bug's family, degenerate-clobber 4, … → basins → canonical/retired vocabulary), kept as a MAP not a gating system per dogfood council `council_c2d959d1` ("kill the overbuilt lens, keep the invariant as executable guards" = moves-substrate-2.0)) shipped 2026-05-29, and v1.7.61 (eval/launchpad record the DISPATCHED antigravity model — recorded==dispatched closed on the eval path: `eval-run` recorded `target_model=config.model`, but agy ignores config.model (no `--model` flag — `/model` in `~/.gemini/antigravity-cli/settings.json` is what runs), so a Gemini 3.5 Flash eval was stamped "Gemini 3.1 Pro"; new shared `providers.read_agy_active_model_raw()` + `dispatched_model(config)` are the single source — `evals/runner.py` records through it and `launchpad_data.py` now reads through the same helper instead of a duplicate file-read; +3 guards; manifest `antigravity.model` bump to "Gemini 3.5 Flash" remains the one manual founder step for the new-model card) shipped 2026-05-29, and v1.7.62 (launch-hardening batch — a council gap-review + a 5-group tackle-workflow drove a workflow-drafted, full-suite-verified sweep: (1) provider-name canonicalization — `_LEGACY_PROVIDER_ALIASES` learned the web-capture brand names (chatgpt→codex, claude_ai→claude, openai/gpt/google/bard/anthropic/claude.ai) so the load-boundary normalizer canonicalizes all 547 council outcomes AT READ (winner stat now per-lab: codex 49% / claude 44% / antigravity 7%, not chatgpt-44%-vs-codex-5%); raw files keep brand names as provenance, `model` field untouched so model-grain comparison survives; NOT the v1.7.40 bug — a capture-path gap (`_LEGACY_PROVIDER_ALIASES` held only `gemini`); (2) #230 clobber-guard propagation to save_registry/basins(+atomic)/lenses/routing-patterns + build_me empty-chairman preserve; (3) #235 embedding backfill (`backfill_prompt_node_embeddings`) + coverage floor-guard (backfill had stalled since 2026-05-12 → 66% unembedded); (4) #226 curl-primary install + Codex TOML fix + #232 install.sh honesty (broken download-embedder note / false Web-Store claim / capture false-green); (5) #234 lens-refresh cross-process lock + #238 degraded-first-run honesty; (6) #231/#237 telemetry no-PII + card-redaction shipped TEST-FIRST (`test_telemetry_no_pii.py` encodes the categorical-only/elo-disclosed/browser-gate/no-card-leak contract, xfail pending impl — telemetry stays default-ON but provably no-PII)) shipped 2026-05-29, and v1.7.63 (#222 — dropped the two dead autofill smoke surfaces (1b content-quality + 11 apply) from `scripts/browser_smoke.py`; both tested the autofill panel intentionally removed in `2934195` — Surface 11 hard-failed "no suggestion items rendered after focus" (the founder-spotted regression), 1b passed vacuously; smoke gate 34→32, feature stays removed) shipped 2026-05-29, and v1.7.64 (#241 embedding speed — pin `device="cpu"`: the "MLX" backend is actually sentence-transformers + torch; nomic-v1.5 `trust_remote_code` ops are MPS-incompatible and the Metal path WEDGED a live backfill from ~56 nodes/s → ~12 nodes/MIN after a GPU "innocent-victim" command-buffer error. Measured fresh on an M1 Ultra: CPU 56 nodes/s + 3s load beats MPS 97/s + 77s load + wedge-fragility, so `_load` pins `device="cpu"` (`TRINITY_EMBED_DEVICE` override) → 33k backfill in ~10min reliably + 3s interactive load; +2 guards `test_embed_device_pin.py`; real-MLX embeddings + cold-start decoupling are #241/#242 follow-ons) shipped 2026-05-29, and v1.7.65 (device-aware embedder #241 — corrects v1.7.64's UNCONDITIONAL `device="cpu"` (which forced slow CPU even on a CUDA box): now explicit `TRINITY_EMBED_DEVICE` > CUDA-if-available (Linux/Windows NVIDIA/ROCm — full PyTorch op coverage, fast+clean) > CPU (Apple-safe), and NEVER auto-MPS (nomic's trust_remote_code ops wedge on Metal). Founder reframe: nomic STAYS (Matryoshka `vector[:dim]` + 8k context are why it's chosen; the only problem was the Apple-MPS runtime, not the model or torch) and torch STAYS (it IS the cross-platform GPU path — MLX is Apple-only, so sunsetting torch would cost Linux/Win GPU users their fast path; MLX is an optional Apple-GPU accelerator, not a replacement); guards updated to cuda-when-available / cpu-otherwise-never-mps / env-override) shipped 2026-05-29, and v1.7.66 (real Apple-MLX embedder #244 — the end of the embedding saga: `embeddings/backend_mlx_native.py` (mlx-embeddings) runs `nomic-ai/modernbert-embed-base` natively on Apple Silicon at ~6300 nodes/s; the selector prefers it → torch fallback (`backend_mlx.py`, now also modernbert so vectors are model-consistent; standard ModernBERT arch → MPS-clean, no `nomic_bert` wedge) → TF-IDF. Model chosen by MEASUREMENT (#243): modernbert-embed-base beat the MLX-able field — Qwen3-0.6B (200× slower), EmbeddingGemma (license-gated), gte-modernbert (70× slower), bge-m3 (won't load) — on Trinity's constraints (MLX-fast · ungated · 8192 ctx · Matryoshka · 94% NN-equiv to v1.5). One root (`nomic_bert` custom arch) had caused the MPS wedge + MLX-unsupported + CPU thrash; swapping the model dissolved all three (principle #29). `[mlx]` extras install mlx+mlx-embeddings on Apple (markers) / torch elsewhere; `TRINITY_EMBED_MODEL` overridable; live corpus re-embedded in place (53,326 → 100% modernbert, compacted, `.v15bak` backup, cosine(stored,fresh)=1.000); +7 guards; macOS NLEmbedding evaluated + rejected (31 nodes/s, 44% agreement). principles.md +#26–#30 from this arc (sample-the-distribution, name-becomes-believed-fact, rank-by-your-constraints, one-root-many-symptoms, right-size-the-fix)) shipped 2026-05-29, and v1.7.67 (corpus-purity floor-guard #245 — a 2026-05-12 bulk ingest had leaked 24,708 non-user prompts (46% of the corpus, mostly Trinity's own `You are extracting durable facts…` extractor scaffolding) past a since-tightened `_is_user_facing_prompt`, poisoning basins/lens/cold-open until a re-apply purged the live corpus to 28,618 real prompts; this ships the standing GUARD — a `real_corpus` test re-runs the boundary filter over the LIVE `prompt_nodes.jsonl` (source-level, not the downstream basins the junk-drawer guard covers) and fails if >5% is scaffolding; mutation-verified (clean corpus 0.0%, pre-purge backup 46.3% → fires); confirmed both live PromptNode writers (`incremental_ingest` + `ingest_helpers`) already route through `iter_prompt_turns`→the filter, so the leak was a now-retired bypass), and v1.7.68 (corpus-size-aware basin count #245 follow-on — the purge's second-order effect the topic-map guard caught: a fixed `k=20` junk-drawers on a large corpus. Removing the 46% scaffolding *concentrated* the real prompts, so re-clustering the clean 28,618-prompt corpus at k=20 packed 29.6% of prompts into one b00 basin — over the 20% junk-drawer ceiling. `auto_k(n_prompts)` (`me/basins.py`) sizes the basin count to the corpus (≈1 basin per 650 prompts, clamped [20,60]); `compute_basins`/`stage1_basins`/`build_me_via_lens_pipeline`/`lens --k-basins` default to `k=None` (auto), explicit k still wins. Sized by PROMPT count (what the junk-drawer guard measures), not thread count — a few long multi-turn export threads let one basin dominate prompt-share while looking small in thread-share. Floors at k=20 below ~13k prompts (behaviour-preserving for fresh installs). Auto-k picks 44 → top basin 19.5%; live `topics.json` rebuilt, real topic spread surfaces (account/product/email · AI-usage · creative-writing · frontend · smart-home); guards in `test_basin_auto_k.py`), and v1.7.69 (surface the council value proof #236 — the council-first painkiller, surfaced from data Trinity already has: no new eval, pure aggregation over the 551 council outcomes on disk. Headline: across [redacted corpus stat] the synthesized winner differed from the user's default 56% of the time (their default was Claude 498/551) — that's how often a single-tab habit would have shipped the worse answer; per-lab split GPT 49% · Claude 44% · Gemini 7% (canonicalized at the load boundary). `personal_routing.council_value_proof()` computes `{changed_pct, win_split, comparable, n}` from `_scan_outcomes()` (+`primary_provider` added to the scan record), returns `{ready:False}` below 10 councils (confidence-honesty n<3-suppress generalized). Surfaced no-new-verb (Q4): a `status` "Council value" line + `council_value` JSON key, a launchpad hero card (`councilValue` page-data + `v-if` block beside the cold-open, self-hides on a thin ledger, brand names not slugs); per-task-type splits deliberately NOT surfaced (too thin — n=1–9 across 423 types); guards in `test_council_value_proof.py`), and v1.7.70 (council value proof — real-contest filter + category wedge #236 — a data-sampling pass on v1.7.69 (founder: "sample the categories each model won so we know we're doing it right") found 36% of the [redacted corpus stat] aren't real contests (only 1 of 3 members gave a ≥200-char answer — flaky-capture-era leftovers whose winner won by default), but restricting to the 354 real contests barely moves the headline (56%→56%, split 49/44/7→48/47/5), so the proof wasn't inflated, just now defensible. `council_value_proof()` filters to ≥2 substantive members (`_scan_outcomes` record gains `substantive_members`); the new `council_category_wedge()` coarsens the 400+ near-unique task_type labels to the head token and names a leader only where a family clears volume (≥8) + margin (≥3) floors → product→GPT (+10), strategic→Claude (+12), architecture→Claude (+4) (Claude wins deliberation, GPT wins generation, Gemini wins no category); surfaced on `status` "by kind:" + the launchpad card; +3 guards), and v1.7.71 (basin scaffolding filter + dedup-at-clustering #248 — first fix from the #240 pipeline audit (BASINS were DEGRADED). Corpus-wide scaffolding was 1.7% (passing #245's 5% floor) but had CONCENTRATED into near-pure scaffolding basins surfacing as fake topics — b19=94% (`# AGENTS.md` dumps), b22=90% (`<environment_context>`), b21 (Trinity's own `Find … this human …` extraction prompts), all leaking 100% through `_is_user_facing_prompt`. Filter tightened to catch them + slash-command skill bodies (`# /loop — …`, the harness-injected command definition, repeated 283× in one basin); `compute_basins` now dedups exact-duplicate texts at the clustering boundary (#15 — "continue" ×130 etc. formed a pseudo-cluster; STORE keeps every node, only clustering input deduped). Re-purged 1,039 scaffolding nodes (28,618→27,579); post-purge junk-drawer spiked to 24.5% (42% dups) then dropped to 14.5% once dedup collapsed the repeats — 32 basins over 20,975 unique texts, zero scaffolding-dominated basins. Guards: `test_no_basin_is_scaffolding_dominated` (real_corpus, fails if any basin ≥20 is >50% scaffolding — corpus-wide floor necessary-not-sufficient, principle #3) + `test_ingest_scaffolding_filter`), and v1.7.72 (the lens sees recent data again #252 — founder caught a stale #1 tension ("[redacted user prompt] I can copy paste" was a 2023 IDE-era workaround, not durable taste). ROOT CAUSE: `collect_turn_pairs` iterates the corpus chronologically + capped at the first 200 pairs, so Stage 0 only ever saw 2023-08→2025-03 — the recent 14 months (64-90% assistant-text) were never reached; the lens was structurally frozen in the past. FIX: `collect_turn_pairs` now walks most-recent-first, capped ≈limit/10 per calendar month so no burst fills the window (real corpus: 20 pairs each from the last 10 months) — durable tensions still recur there (re-confirmed), phases that ended decay via the registry's 90-day fade. Plus the canonical time field `me/chapters.prompt_time` (=timestamp-or-created_at; `created_at` is ingest day, collapsed 38 months→1) + `me/chapters.detect_chapters` (deterministic datable topic surges — smart-home build 2025-06→07, OZ-deal arc 2025-05→2026-02, frontend phase that ENDED 2023-11→2024-07) surfaced on `status`. Build-step-1 of the decay-aware lens; registry recency-weighting + chapter-spread robustness is step-2. Guards: `test_chapters` (+ real_corpus time-spread floor) + `test_turn_pair_recency`), and v1.7.73 (agent-ops noise filter #252-validation — founder asked to sample the last month to check the consume-latest decision; it caught that 2026-05 (the latest month) is NOT representative taste but a Trinity-building sprint: 29% dev/meta, 16% explicit agent-ops control ("respond with the word HELLO and nothing else", "continue with the plan if currently paused" — the loop's own automation/dispatch-tests), only ~11% real-life taste; clustering last-month-only put 5 of 8 basins on dev/agent-ops. So naïve "consume latest" would profile the user as a Trinity dev — recency necessary-not-sufficient, needs noise-filter + topic-diversity (#242 cold-start design updated to recent-but-diverse). `_is_user_facing_prompt` now drops the two control SHAPES (output-format probes + automation continuations) while keeping bare "continue"/"ok"/"continue this story"/"respond to John" (tested); caught 354 residual rows, re-purged 27,579→27,225. Surfaced a follow-on (folded into #255): v1.7.71's dedup feeds `auto_k` the deduped count → 650-ratio ~30% too coarse → dominant 'user/product/account' basin 24.7%; live topics.json rebuilt at k=40 (16.3%) stopgap, durable auto_k retune + cohesion gate ride with #255) shipped 2026-05-29 — pyproject `<!-- canonical:version -->1.7.73<!-- /canonical -->`; <!-- canonical:test_count -->2304<!-- /canonical --> tests passing + <!-- canonical:skipped_count -->4<!-- /canonical --> skipped (commit `2bbb333` regenerated `docs/launchpad_example.png` post Phase-3d UI swap and cleared the formerly-intentional fail from the tightened `TestLaunchpadScreenshotFreshness` guard); <!-- canonical:doc_consistency_guards -->108<!-- /canonical --> doc-consistency guards green; <!-- canonical:mcp_tool_count -->8<!-- /canonical --> MCP tools live; <!-- canonical:cli_command_count -->48<!-- /canonical --> CLI subcommands across <!-- canonical:command_module_count -->21<!-- /canonical --> user-facing command modules (20 in `CORE_COMMAND_MODULES` + `install` in `OPTIONAL_COMMAND_MODULES`); <!-- canonical:smoke_surface_count -->32<!-- /canonical -->-surface browser smoke gate passing (`python scripts/browser_smoke.py`).

## Auto-Dream coexistence (load-bearing positioning)

Anthropic shipped official **Auto-Dream** in Claude Code — 24h+5-sessions trigger, 4-phase REM-mirror, 200-line MEMORY.md cap (<https://claudefa.st/blog/guide/mechanics/auto-dream>). They've also shipped **Memory + Dreaming for Managed Agents** with files-as-memory + out-of-band curation (Harvey 6× legal benchmark, Rakuten 97% error reduction). Trinity's `dream` verb collides; the public differentiator is **cross-provider data sovereignty**, not architectural elegance.

**Two audiences, two leads, one architecture underneath:**

- **README / public framing** = "Own your taste." The moat is the corpus — Trinity reads transcripts from Claude + Codex + Antigravity + Cursor + claude.ai + chatgpt.com + Gemini, locally. The three labs are commercially prevented from crossing: Anthropic can't recommend ChatGPT, OpenAI can't recommend Claude, Google can't recommend either. That asymmetry is unfakable. Anthropic could ship Trinity's exact recursive gate tomorrow and still couldn't see across providers.
- **CLAUDE.md / internal framing (this file)** = the lens is the source of truth and the chairman is the procedural compiler. The chairman that synthesizes a council reads `lens.md` tensions inline and derives procedural guidance at inference time — no separate moves substrate needed. The moves layer shipped in #167–#172 + #181 was demoted to dormant code in #184 after a real-data dream cycle proved the gate was structurally inactive (T1 lexical primitive applied surface-form logic to a problem requiring semantic-space logic). The recursion edge that DOES live: council outcomes feed back into lens-build over time (basins where chairman picks lose drift toward re-extraction). Cross-provider data sovereignty is what no lab can fake; the architecture is allowed to be simple.

This file applies Anthropic's 200-line MEMORY.md discipline to itself; historical context lives in `docs/historical/`.

## Architectural commitments (load-bearing, not negotiable)

1. **No LLM calls outside councils.** Ingest, embedding, theme assignment, search ranking, clustering — pure embeddings + heuristics + metadata. The only LLM invocations Trinity makes are council member calls and chairman synthesis calls, both riding user subscriptions.
2. **Prompt content never uploads.** Even with v1.1 aggregation enabled, only categorical routing labels (`task_type`, `provider_scores`, `winner`) leave the machine. Anonymous, opt-in only.
3. **Local-first inference.** Phase 9's learned router runs on the user's hardware. No hosted controller. No per-call API billing.
4. **Subsidized consumer credits as cost basis.** Trinity dispatches via the user's own CLI subscriptions (Claude Code, Codex, Antigravity). If anyone proposes a hosted API tier, push back hard — that destroys both cost basis and privacy.
5. **HF Hub offline by default.** `main()` pins `HF_HUB_OFFLINE=1` + `TRANSFORMERS_OFFLINE=1` via `setdefault` at startup. The embedding model is pulled once via `huggingface-cli download nomic-ai/nomic-embed-text-v1.5`; after that Trinity loads from `~/.cache/huggingface/hub/` and never contacts the Hub during normal operation.

## MCP surface

### The eight MCP tools (`mcp_server.py`)

The full public surface is **<!-- canonical:mcp_tool_count -->8<!-- /canonical --> tools** — 4 canonical + 3 v1.5 additions + 1 in-protocol provider-side loop (`import_provider_memory`). Plus MCP Resources (lens + scoreboards exposed at session handshake — see "MCP Resources" section below). Tool docstrings ARE the contract the agent reads at handshake. (`handoff` retired 2026-05-26 — 0 production usage events; lens flows via MCP Resources instead.)

**Canonical four (lifecycle order):**

1. **`route(task, harness?, available_models?, budget?, latency?, current_provider?)`** → `{mode, primary, challenger, confidence, reason, fallback}`. No model calls — heuristic + k-NN + chairman picker. Cheap; call before the harness picks a model. `budget` ∈ {`low`, `normal`, `high`}; `latency` ∈ {`fast`, `normal`, `patient`}. **Deprecated as of #213 (Q4 surface-collapse): prefer `ask(mode="route")`, which returns the identical decision; `route` stays registered for back-compat (kept in the count of 8).**

2. **`run_council(task, goal?, members?, mode?, sequence?, primary_provider?, responses?, wait_seconds?)`** → council launched asynchronously. `mode="parallel"` (default) runs members concurrently then chairman; `mode="chain"` runs sequence serially. When `responses=[...]` is provided, skips dispatch and runs chairman synthesis only (subsumes the legacy `judge` tool, formerly registered). `wait_seconds > 0` blocks up to that many seconds for the outcome inline; otherwise returns the `council_run_id` and the caller polls `get_council_status`.

3. **`get_persona()`** → returns `~/.trinity/memories/lens.md`. Exposes the persona once at session start so any harness can tailor responses without an MCP round trip per call.

4. **`get_council_status(council_run_id)`** → in-protocol polling for async councils. Returns status (running/completed/failed/canceled), per-member progress, synthesis state, and outcome summary (winner, agreed/disagreed claims, routing_lesson) when complete.

**v1.5 trio:**

5. **`ask(query, mode?, available_providers?, top_k?, budget?, latency?, current_provider?, harness?)`** → cheap single-call default routing. The 90% case: one call, one provider, chairman-blessed verdict. Pulls from cortex picks first (high-trust rule → use directly), falls back to k-NN advisory, finally to heuristic. `mode="route"` (#213 Q4) returns the routing decision only (no model call) — subsumes the deprecated `route` tool.

6. **`get_picks(basin_id?, min_trust?)`** → agent-facing introspection into extracted cortex routing patterns. Returns `{rules: {basin_id: pattern}, total_basins, returned}` — patterns carry provider/reasoning/trust score/source councils. `basin_id` narrows to one basin; `min_trust` floor filters by `trust_score.value`.

7. **`mark_pick_wrong(basin_id, reason?, reset?)`** → user-veto on an extracted pick. Each call increments `override_count`; `effective_trust = raw_trust × 0.5^count`. Persists across consolidations — a fresh extraction can't erase the user's signal. `reset=true` clears the count.

**Launch-arc additions:**

8. **`import_provider_memory(kind, payload, provider?, dry_run?)`** → in-protocol provider-side loop. The agent inside Claude Code / Cursor / Codex has the user's conversation history on its side; this tool lets it pipe extracted lens tensions or REFRAME/REDIRECT/SHARPENING/COMPRESSION rejection signals straight into `~/.trinity/me/lenses.json` (kind=`lens`) or `~/.trinity/me/rejections.jsonl` (kind=`eval`) without copy-paste. Schemas match `docs/lens-from-provider.md` and `docs/evals-from-provider.md`; same dedup/append-only semantics as `trinity-local lens-import` / `eval-import`. CLI mirrors: `lens-prompt` / `lens-import` / `eval-prompt` / `eval-import`.

(`record_outcome` retired 2026-05-21 — chairman's pick is the supervision signal now. `get_eval_summary` retired 2026-05-18. See `docs/historical/retirement-log.md`.)

**Actionable rule for agents in this repo:** before calling `AskUserQuestion` with multiple options that are NOT user-personal preferences, FIRST run `mcp__trinity-local__ask` or `mcp__trinity-local__run_council`. Treat `AskUserQuestion` as reserved for *user-personal* choices; Trinity is the default for *product/architectural* choices. Skip Trinity for trivial bugs, syntax lookups, mechanical refactors, information retrieval. After a council, treat the chairman's `agreed_claims` / `disagreed_claims` as the source of truth.

### State layout

Live state under `~/.trinity/`. Two conventions: **entities** use JSON-per-file under a named directory; **event logs** use append-only JSONL. Anything not on this diagram is either retired (see `docs/historical/retirement-log.md`) or written by a feature you haven't run yet.

```
~/.trinity/
├── conversations/                  # Chrome-ext Native-Messaging captures (claude.ai / chatgpt.com / gemini.google.com)
├── todos/                          # Durable todo records (the directory name; `tasks_dir()` migrates legacy `tasks/` → `todos/`)
├── actions/                        # Pending action records
├── prompt_bundles/                 # Saved prompt bundles
├── council_outcomes/               # Council outcome JSON (routing_label + chain_steps) — canonical supervision ledger
├── reviews/                        # Post-hoc review JSON
├── review_pages/                   # Static HTML review pages
├── portal_pages/                   # Static launchpad (file://); status/ holds live council progress
├── task_sync/                      # Sync-safe task payloads
├── share/                          # PNG share-card outputs (me-card / council-share / eval-share defaults)
├── evals/                          # Eval sets (eval-build) + per-run results (eval-run)
├── settings/                       # Telemetry settings
│
├── prompts/                        # Raw prompt index (renamed from memory/; one-time migration in prompts_dir())
│   ├── prompt_nodes.jsonl          #   PromptNode index (tier 1)
│   ├── turn_windows.jsonl          #   TurnWindow index (tier 2 — local context)
│   └── cursors.json                #   Per-source ingest cursors (consumed by ingest_recent())
├── memories/                       # Cognitive lens (the three thinking files)
│   ├── lens.md                     #   paired tensions (value)
│   ├── topics.json                 #   subject basins (semantic + lens evidence map)
│   └── vocabulary.md               #   anchors + homonyms + synonyms (linguistic)
├── core.md                         # Singular distillation chairman reads first
├── scoreboard/                     # Operational scoreboards (NOT cognitive memory)
│   ├── picks.json                  #   extracted model-selection rules per basin
│   └── routing.json                #   per-task-type provider track record
├── me/                             # Lens-build pipeline output (rejections/decisions/lenses/orderings/merges/lens_registry)
│
├── outcomes.jsonl                  # Per-session outcome records (drift)
├── council_runs.jsonl              # Council outcome log
├── launch_events.jsonl             # Launch/handoff events
├── council_feedback.jsonl          # User verdicts feeding the personal routing table
├── analytics/                      # routing_label_events / knn_advisory / dispatch_outcomes
├── cold_start_scan.json            # First-spawn auto-scan
└── research/                       # Optional user-curated k-NN advisory corpus (read-only for Trinity)
```

### Glossary (load-bearing terms)

- **prompts** — what the user owns (raw, indexed in `~/.trinity/prompts/`). Inputs to `dream`.
- **dream** — the verb. Reads prompts, emits core memories (offline, your data). Cross-provider analog of Anthropic's Auto-Dream (see callout above).
- **core memories** — three *thinking* memories that compose the lens. Chairman reads `core.md` (identity) first, drills to `lens.md` (paired tensions), `topics.json` (subject basins), or `vocabulary.md` (linguistic anchors) on demand. Generation runs bottom-up; reads run top-down. Live under `~/.trinity/memories/` (with `core.md` at `~/.trinity/core.md`).
- **scoreboards** — operational bookkeeping at `~/.trinity/scoreboard/` (`picks.json`, `routing.json`). Excluded from chairman context; surfaced on the launchpad routing card. Computed from `council_outcomes/`.
- **council** — multi-model deliberation (parallel or chain) ending in chairman synthesis.
- **chairman** — the synthesis model in a single council. Reads `core.md`, emits structured Routing JSON.
- **Conductor** (v1.5+) — flagship model that picks which model gets which sub-task across a session. Different role than chairman; same model family may play both.
- **member** — a provider acting as one voice in a council. Canonical term in code AND copy.
- **harness** — the CLI/IDE the user is in (Claude Code, Codex CLI, Antigravity, Cursor). Trinity registers as an MCP server inside each via `install-mcp`.
- **task_type** — short label for "what kind of question this is" (heuristic on input, also emitted by chairman). NOT the same as `category` (coarser LMArena-aligned grouping).

### Provider trio across layers

Same lab gets a different name at each layer; what users see depends on the entry surface.

| layer | Anthropic | OpenAI | Google |
|---|---|---|---|
| mobile app brand | Claude | ChatGPT | *(no Antigravity mobile yet)* |
| cloud agent harness | Claude Code | Codex agents | *(no equivalent)* |
| desktop CLI binary | `claude -p` | `codex exec` | `agy -p` |
| Trinity slug (code/config/JSON) | `claude` | `codex` | `antigravity` |
| underlying model | Claude Opus 4.8 / Sonnet 4.6 / Haiku 4.5 | GPT-5.5 | Gemini 3.1 Pro Preview |

Use **slugs** in code/config/file paths/JSON keys. Use **model names** in user-facing UI. Use the mixed marketing trio ("Claude, Codex, and Gemini") in launch copy.

## Architecture pointers

CLI dispatcher: `src/trinity_local/main.py` (thin; `set_defaults(handler=...)`). 22 user-facing command modules in `commands/` (21 CORE + `install` OPTIONAL). Notable: `dream.py` (cold-start verb), `me.py` (`lens-build`), `council.py` (`council-start` / `council-launch` / `council-stop` / `council-share` / `council-iterate`), `cortex.py` (`consolidate` + `cortex-override`), `eval.py` (`eval-build` / `eval-run` / `eval-show` / `eval-share`), `extension_repair.py` (`extension repair` — diagnose capture failures and dispatch a council to propose a page-hook.js patch; the "Trinity heals itself" flagship demo. `--auto` (#147) skips the HAR ask and auto-dispatches when a code-patch pattern surfaces; the launchpad's "Repair extension" button fires `--auto --json`.), `import_export.py` (`import-export` — auto-detect + bulk-import Takeout / web exports from any path, task #148; engine in `src/trinity_local/ingest_helpers.py`), `install.py` (`install-mcp` writes Claude Code / Codex / Antigravity / Cursor configs — 4 CLI harnesses), plus `debug.py`, `download_embedder.py`, `install_umbrella.py`, `me_card.py`, `portal.py`, `review.py`, `status.py`, `telemetry.py`, `update.py`, `vocabulary.py`, `watch.py` (`ingest-recent`). (Modules retired 2026-05-26: handoff + memory_compare. Retired 2026-05-27: `commands/adapters.py` (duplicated `status`), `commands/decision_log.py` (interactive logger with 0 organic uptake; `me/decisions.py` loader survives), `commands/replay.py` (`replay-history` cold-start backfill — routing table populates from normal council usage now), `commands/seed.py` (`seed-from-taste-terminal` required a personal-rig directory layout no end user has; `import-export` replaces it; helpers consolidated into `ingest_helpers.py`). See retired_names.py.)

Core layers (`src/trinity_local/`, selected — full tree is <!-- canonical:py_file_count -->125<!-- /canonical --> `.py` files; `find src/trinity_local/ -name "*.py"`): `mcp_server.py` (MCP entrypoint), `ask.py` (the v1.5 `ask` MCP handler — cheap single-call routing), `council_runner.py` + `council_runtime.py` + `council_schema.py` + `council_status.py` + `council_review.py`, `personal_routing.py` (chairman-pick aggregation feeding the routing table — load-bearing post-rating-retirement), `memory/` (`schemas.py`, `store.py`, `index.py`, `replay_value.py`), `embeddings/` (`backend_mlx.py`, `backend_tfidf.py`), `ingest.py`, `categories.py`, `ranker/` (`base.py`, `fallback.py`, `heuristic.py`, `knn_ranker.py`, `chairman_picker.py`, `types.py`), `cortex_geometry.py` (geometric median + bimodality + participation-ratio for cortex consolidation; dependency-free stdlib), `me_builder.py` (lens-build pipeline) + `me_lenses.py`, `evals/` (`builder.py` + `scorer.py` — task #122 corpus-based eval harness), `health_checks.py` (cold-install + post-install health checks; the `doctor` CLI verb retired pre-launch, so the module was renamed 2026-05-27 to match its actual job — `trinity-local status` calls into it), `drift.py` (drift surfacing on launchpad), `design_system.py` (shared CSS + Vue3 chip vocabulary for launchpad/memory-viewer/review pages), `state_paths.py`, `runtime_env.py` (PATH-injection env builder; `subprocess_utils.py` was planned but the split didn't materialize), `task_types.py` (`guess_task_type()`), `refresh.py`, `dispatch_registry.py` + `capture_host.py` (the macOS-Shortcut dispatch tier — `shortcuts_integration.py` / `shortcut_setup.py` / `dispatch_runner.py` — fully retired 2026-05-26; Native Messaging via the Chrome extension is the canonical dispatch path now), `launchpad_data.py` + `launchpad_template.py` + `launchpad_runtime.py` + `launchpad_page.py` + `memory_viewer.py`, `share_card_base.py` + `me_card.py` + `eval_card.py` + `council_card.py`, `council_feedback.py`, `telemetry.py`, `notifications.py`, `adapters.py`, `config.py`, `providers.py`, `utils.py`, `retired_names.py`.

## Install / surface notes

Run `trinity-local install-mcp` once to register Trinity's MCP server with the four CLI harnesses (Claude Code → `~/.claude.json`, Antigravity → `~/.gemini/settings.json`, Codex CLI → `~/.codex/config.toml`, Cursor → `~/.cursor/mcp.json`). Each harness spawns `trinity-local --mcp` as a stdio child; ~62MB resident while connected. The launchpad → Chrome extension → Native Messaging → `trinity-local-capture-host` → CLI pipeline is independent of MCP (one-shot subprocess at every step).

## MCP server hot-reload (development only)

Set `TRINITY_MCP_WATCH=1` to enable a file watcher that calls `os._exit(0)` on any `.py` change. The MCP launcher auto-respawns. Never enable in shipped configs. Adding *new* MCP tools (vs. modifying existing handlers) may require a Claude Code restart to make them visible.

## Coding conventions

- **Python 3.10+**. `from __future__ import annotations` in every module for PEP 604 style.
- **Dataclasses everywhere**. No Pydantic, no attrs. Manual `to_dict()`.
- **Minimal runtime dependencies.** Three: `Pillow>=10` (PNG share cards), `mcp>=1.0` (MCP server runtime — `install-mcp` registers Trinity in Claude Code / Codex / Antigravity / Cursor), `numpy>=1.26` (lens-build cosine matmul, k-means in `me/basins.py`, vocabulary stats). `[mlx]` extras add the embedder deps: `mlx` + `mlx-embeddings` on Apple Silicon (the real Apple-MLX path, #244 — `nomic-ai/modernbert-embed-base` at ~6300 nodes/s) plus `sentence-transformers>=2.2`, `einops`, and `torch>=2.0` (the cross-platform fallback, same model on the standard ModernBERT arch) for real 768d embeddings; without any of them the embedder falls back to the stable SHA-1 TF-IDF projection. `[test]` extras add `pytest>=7`.
- **Stable IDs** via `stable_id()` (`utils.py`) — `sha1(prefix|parts...)[:16]`.
- **JSONL append logs** for analytics; JSON entity files for objects.
- **`to_dict()` filters None / empty strings / empty containers**.
- **`now_iso()`** — UTC ISO 8601 with `microsecond=0`. **`trinity_home()`** — `~/.trinity/` (or `$TRINITY_HOME`).
- **Graceful degradation** — features depending on `[mlx]` fall back silently. Analytics never crash.

## Launch arc

The five-workstream distribution arc and full forward-arc narrative now live in [`docs/historical/brand-evolution.md`](docs/historical/brand-evolution.md) and [`docs/historical/scale-plan.md`](docs/historical/scale-plan.md). Known-shipped workstreams: task #114 (MCP-dropdown distribution), #115 (First-run wow / 60s cross-provider continuity demo), #117 (Standardize `~/.trinity/`), #118 (Subsidy-window narrative), #122 (Corpus-based eval harness), #135 (gemini.google.com Chrome-capture adapter), #136 (`trinity-local extension repair` flagship demo), #162 (MCP Resources). The v2 moves substrate (#167–#172, #181) was retired 2026-05-27 in #184 after a real-data dream cycle proved the 4-tier gate structurally dormant — chairman LLM bridges declarative→procedural at inference time, making moves a redundant projection. Active follow-ups: #182 trajectory lens, #183 schema versioning, #185–#189 testing/orphan-detection infrastructure. The `principles.md` pipeline remains data-gated (needs ≥100 council outcomes). (Tasks #119 handoff + #142 memory-compare retired 2026-05-26 — see retired_names.py.)

## Verified status

- `pytest -q` — **<!-- canonical:test_count -->2304<!-- /canonical --> passed** + <!-- canonical:skipped_count -->4<!-- /canonical --> skipped (167s wall, all gated real-Chrome smokes intentionally skipped). <!-- canonical:doc_consistency_guards -->108<!-- /canonical --> doc-consistency guards in `test_doc_count_consistency.py` defending launch-credibility claims. The screenshot-freshness guard (`TestLaunchpadScreenshotFreshness::test_launchpad_example_not_grossly_stale`) is the canary — it fires when `docs/launchpad_example.png` falls more than a day behind `launchpad_template.py`; clear it with the regen recipe in the test's error message.
- `trinity-local --mcp` exposes <!-- canonical:mcp_tool_count -->8<!-- /canonical --> tools (canonical 4 + v1.5 trio + in-protocol `import_provider_memory`) plus 6 MCP Resources (lens + scoreboards).
- `trinity-local install-mcp` writes 4 CLI harnesses (Claude Code, Antigravity, Codex, Cursor).
- `python scripts/browser_smoke.py` — <!-- canonical:smoke_surface_count -->32<!-- /canonical -->-surface browser smoke gate green.

## Development

- Run tests: `.venv/bin/python -m pytest tests/ -v`
- Isolated state: `TRINITY_HOME=/tmp/trinity-test PYTHONPATH=src python3 -m pytest -q`
- Embeddings require the MLX extras (`pip install -e '.[mlx]'`); without them the embedder uses the SHA-1 TF-IDF fallback.
- `AGENTS.md` is a thin redirect to here so Codex / other agent harnesses don't drift.

## Historical context (read these when you need depth)

- [`docs/historical/principles.md`](docs/historical/principles.md) — 25 numbered meta-principles extracted from the fixes. Each earned its place by costing time.
- [`docs/historical/retirement-log.md`](docs/historical/retirement-log.md) — what Trinity used to have, why it was retired, when. Canonical registry is `src/trinity_local/retired_names.py`.
- [`docs/historical/brand-evolution.md`](docs/historical/brand-evolution.md) — pivot trail to "Own your taste"; ratifying councils; v1.7.5 Auto-Dream-coexistence framing.
- [`docs/spec-v1.md`](docs/spec-v1.md) — locked v1.0 launch spec.
- [`docs/historical/spec-v1.5.md`](docs/historical/spec-v1.5.md) — active next-trajectory spec.
- [`docs/historical/spec-v1.6.md`](docs/historical/spec-v1.6.md) — browser-extension + Native-Messaging spec.
- [`docs/three-tier-architecture.md`](docs/three-tier-architecture.md) — MCP server (primary) / Pip engine / Chrome Extension; `~/.trinity/` is the invariant data contract.
- [`docs/historical/cross-platform-spec.md`](docs/historical/cross-platform-spec.md) — terminal → desktop → mobile phasing.
- [`docs/historical/scale-plan.md`](docs/historical/scale-plan.md) — long-form roadmap.
- [`docs/product-spec.md`](docs/product-spec.md) — feature-level spec.
- `CHANGELOG.md` — timestamped append-only.
