<!-- Thanks for the PR! A short scope description here makes review fast. -->

## What this changes

<!-- One paragraph. What the user can do after this PR that they couldn't before. -->

## Why this matters

<!-- Optional, but appreciated. If it's a bug fix, mention the bug; if
it's a feature, name the use case. -->

## How it was tested

- [ ] `pytest -q` runs green locally
- [ ] `bash scripts/launch-check.sh` runs green (or the change is doc-only)
- [ ] Manual smoke (if UI / dispatch / install path)

<!-- If you added behavior, name the test that covers it. If a test
isn't possible, say so — silent un-tested PRs are how Trinity's
"every shipped feature gets a smoke regression guard within one tick"
discipline (CLAUDE.md principle #14) gets diluted. -->

## Anything reviewers should know

<!-- Architectural decisions, things you considered and rejected,
follow-up work you'd like to split into a separate PR, etc. -->
